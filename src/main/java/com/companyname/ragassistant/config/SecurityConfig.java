package com.companyname.ragassistant.config;

import org.springframework.context.annotation.Configuration;

/**
 * Security intentionally disabled for MVP.
 * We can enable Spring Security later (JWT/API keys/RBAC) when the core RAG flow is proven.
 */
@Configuration
public class SecurityConfig {
}